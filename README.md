# React-приложение для отображения файловой структуры из db.json

# Установка зависимостей для проекта происходит через команду npm install

# Запуск react-server происходит через команду npm run server

# Запуск проекта происходит через команду npm run dev
